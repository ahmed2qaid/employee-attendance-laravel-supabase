# Configuration principles

This branch is intentionally environment-driven. Copy `.env.example` to `.env`, add your own Supabase PostgreSQL connection values, then run Laravel migrations. Never commit `.env` or real database passwords.
